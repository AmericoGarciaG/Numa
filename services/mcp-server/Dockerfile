# MCP Server Dockerfile - Placeholder
# This will be configured when llm-mcp-server-py is cloned

FROM python:3.11-slim

WORKDIR /app

# Install git for cloning
RUN apt-get update && apt-get install -y git && rm -rf /var/lib/apt/lists/*

# This dockerfile will be replaced when the MCP server is properly set up
RUN echo "MCP Server placeholder. Clone llm-mcp-server-py here and configure."

EXPOSE 8001

CMD ["echo", "Configure MCP server first. See TESTING_GUIDE.md"]